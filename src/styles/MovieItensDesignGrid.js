import styled from 'styled-components';

export const MovieItemGrid = styled.div`

    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
    margin: auto;
    width: 70%;
    padding: 30px;
    text-align:center;

   
`